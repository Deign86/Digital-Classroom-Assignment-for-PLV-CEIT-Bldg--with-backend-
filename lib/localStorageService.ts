/**
 * Legacy local storage service stub.
 *
 * The application now relies on Firebase for persistence and authentication.
 * This module re-exports the Firebase service to maintain backward compatibility
 * with older documentation references.
 */
export * from './firebaseService';
