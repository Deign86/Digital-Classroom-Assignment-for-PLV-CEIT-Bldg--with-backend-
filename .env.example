# Supabase Configuration
# Get these values from your Supabase project settings
# Project Settings > API > Project URL and Project API keys

VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Service Role Key (Required for admin operations)
# ⚠️ IMPORTANT: Keep this secret! Never commit to git or expose to client
# Used for: Creating users with auto-confirmed emails, admin operations
# Get from: Project Settings > API > service_role secret key
VITE_SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key
