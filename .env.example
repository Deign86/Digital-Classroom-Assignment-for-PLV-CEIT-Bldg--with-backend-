# Supabase Configuration - Client Side
# Get these values from your Supabase project settings
# Project Settings > API > Project URL and Project API keys

VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# ⚠️ NOTE: We no longer use VITE_SUPABASE_SERVICE_ROLE_KEY on the client!
# 
# The service role key is now ONLY used by Supabase Edge Functions (server-side).
# To set it up:
# 1. Install Supabase CLI: npm install -g supabase
# 2. Login: supabase login
# 3. Link project: supabase link --project-ref your-project-ref
# 4. Set secret: supabase secrets set SUPABASE_SERVICE_ROLE_KEY=your_service_role_key
# 
# See EDGE_FUNCTIONS_SETUP.md for complete deployment instructions.
